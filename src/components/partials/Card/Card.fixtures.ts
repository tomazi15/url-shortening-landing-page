export const mockCardData = [
  {
    id: 0,
    description: "foo",
    title: "bar",
  },
];

export const mockLogo = mockCardData[0].title;

export const mockCardTitle = mockCardData[0].title;

export const mockCardDescription = mockCardData[0].description;
